{% extends 'base.html.twig' %}

{% block title %}Modifier un cours{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Modifier un cours</h1>

        {{ form_start(form) }}
        {{ form_row(form.Matiere) }}
        {# Ajoutez d'autres champs du formulaire selon votre type de formulaire CourType #}
        <div class="text-center">
            <button type="submit" class="add-course-btn">Modifier</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
